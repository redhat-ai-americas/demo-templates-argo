apiVersion: v2
name: trustyai
description: GitOps for deploying a TrustyAI service in OpenShift AI.

type: application

version: 0.1.0

appVersion: "1.0.0"

dependencies:
  - name: cluster-monitoring
    version: 0.1.0
    repository: "file://../cluster-monitoring"
    condition: cluster-url-getter.enabled
