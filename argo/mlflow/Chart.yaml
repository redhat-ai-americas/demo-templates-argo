apiVersion: v2
name: mlflow
description: GitOps for deploying a production-like MLFlow instance with dedicated Postgres backing and S3 artifact storage.

type: application

version: 0.1.0

appVersion: "1.0.0"

dependencies:
  - name: minio
    version: 0.1.0
    repository: "file://../../helm/10-minio"
    condition: minio.enabled
