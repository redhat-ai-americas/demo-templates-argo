apiVersion: v2
name: predictive-lifecycle
description: GitOps to set up a demo for the general  GitOps for obtaining the URL of your OpenShift cluster.

type: application

version: 0.1.0

appVersion: "1.0.0"

dependencies:
  - name: cluster-url-getter
    version: 0.1.0
    repository: "file://../../helm/cluster-url-getter"